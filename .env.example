# =============================================================================
# Uncharted Lands - Server Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# Never commit the .env file to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Port the game server will listen on
PORT=3001

# Host address to bind to
# Use 0.0.0.0 to listen on all interfaces
# Use localhost or 127.0.0.1 for local development only
HOST=0.0.0.0

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# PostgreSQL connection string (shared with client)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Example (local): postgresql://postgres:password@localhost:5432/uncharted_lands
# Note: Server uses the same database as the client
DATABASE_URL="postgresql://postgres:password@localhost:5432/uncharted_lands"

# -----------------------------------------------------------------------------
# Sentry Configuration (Error Tracking & Monitoring)
# -----------------------------------------------------------------------------
# Get these from: https://sentry.io/settings/projects/
# Leave as "disabled" in development if you don't want error tracking

# Sentry DSN (Data Source Name) for error reporting
SENTRY_DSN="disabled"

# Sentry Auth Token for source map uploads during build
# Only needed in CI/CD or when deploying
SENTRY_AUTH_TOKEN="disabled"

# -----------------------------------------------------------------------------
# Game Server Configuration
# -----------------------------------------------------------------------------
# Socket.IO CORS origins (comma-separated)
# Example: http://localhost:5173,https://uncharted-lands.vercel.app
# CORS_ORIGINS=http://localhost:5173

# Game tick rate (ticks per second)
# Default: 60 (for smooth gameplay)
# TICK_RATE=60

# Max players per world
# MAX_PLAYERS_PER_WORLD=100

# -----------------------------------------------------------------------------
# Development Configuration
# -----------------------------------------------------------------------------
# Node environment (automatically set by most tools)
# NODE_ENV=development|production
